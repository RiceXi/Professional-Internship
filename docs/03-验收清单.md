# 任务一验收清单

依据：[根目录任务书](../面向大模型推理优化的操作系统级任务设计_合并版.docx)任务一与通用考核要求。任务书是课程原始要求；本目录是为满足这些要求编写的项目材料。

## 已实现与待补证据

| 验收内容 | 当前状态 | 可检查证据 |
|---|---|---|
| CPU 分页、页表、动态扩展与回收 | 已实现并测试 | `src/memory/paging.py`、`test/cpu/test_memory.py` |
| 连续预分配基线与碎片统计 | 已实现并实验 | `contiguous.py`、129 组原始运行 |
| fork、引用计数与 COW | 已实现并测试 | `test_cow.py`、共享分支图表 |
| 有界 CPU 交换池、LRU、固定工作集 | 已实现并测试 | `test_swap.py`、随机生命周期与失败回滚 |
| 仿真器覆盖率 ≥70% | 已达到；具体数值以归档报告为准 | `experiments/task1/tests-*` |
| 真实 KV 张量后端与引擎接入 | 代码已实现；CPU 张量用例通过 | `torch_storage.py`、`gpu_engine.py` |
| GPU 页往返与真实模型输出对照 | **待在 RTX 4060 Laptop 执行** | `test/validate_gpu.py` |
| 容量、利用率、碎片、共享定量对照 | CPU 实验完成 | [量化评测报告](02-量化评测报告.md) |
| GPU 显存峰值、并发/长上下文压力、OOM、搬运代价 | **待实测** | GPU 验证脚本与压力矩阵入口 |
| 实际对话或长文本轨迹补充实验 | **待提供并运行有来源的负载** | 当前仅确定性合成负载 |
| OS 映射、模块图与时序图 | 已编写 | [系统设计](00-系统设计.md) |
| 一步 CPU 启动和复现脚本 | 已提供并验证 | `scripts/setup_cpu.sh` |
| 量化报告、原始数据与绘图 | 已提供 CPU 结果 | `experiments/task1/`、绘图/报告脚本 |
| 答辩 PPT | 已生成；GPU 部分明确待实测 | [任务一答辩.pptx](任务一答辩.pptx) |
| 五分钟实际演示录屏 | **待录制**；演示顺序已写 | [复现与演示](04-复现与演示.md) |
| 小组分工、例会与代码互审记录 | **待按真实过程整理** | 不能由提交历史代替或虚构 |

目前可以验收本地实现与 CPU 实验。完整课程交付仍需实际 GPU 报告、录屏和真实过程记录。

## 本次可追溯开发记录

以下为仓库开发里程碑，不是学生小组例会或互审记录。

| 提交 | 内容 |
|---|---|
| `0e7f834` | 独立课程定位、任务书和任务一范围 |
| `418eb79` | CPU 分页、连续基线、测试与 CI，删除无关历史优化文档 |
| `290fa2d` | fork/COW 与分支隔离测试 |
| `c90e150` | 有界交换池、LRU 和固定工作集 |
| `655be7f` | 张量后端、课程运行器和真实 GPU 验证入口 |

后续提交可用 `git log --oneline` 查看。每阶段独立提交并推送，回退时可先创建新分支检查指定提交，或使用 `git revert` 保留历史。

## 真实过程记录应包含

按实际发生的内容填写姓名与分工、日期、讨论问题、结论、对应提交/PR、互审意见和修正证据。演示前检查 GPU 报告的设备、提交与源码哈希是否对应最终代码；任何失败或跳过都应保留，不用仿真数据替代。
